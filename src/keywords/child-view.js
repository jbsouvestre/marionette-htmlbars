/**
 * The `{{childViewContainer}}` helper lets you specify where the child view should be rendered
 */

export default {
    willRender(renderNode, env) {

    },

    setupState(state, env, scope, params, hash) {

    },

    childEnv(state, env) {

    }

    isStable(lastState, nextState){

    }

    isEmpty(state){
        
    }
}